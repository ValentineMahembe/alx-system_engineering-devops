#!/usr/bin/env bash
# This script displays a list of currently running processes.

# Use the ps command to list all processes, including those without a TTY.
# The -e option ensures that all processes are displayed.
# The -f option provides a full-format listing.
# The --forest option shows the process hierarchy.
ps -a uf
